import React from "react";

const Button = () => {
  return (
    <div>
      <button
        type="submit"
        className="bg-blue-500 text-white rounded-md px-2 font-bold text-3xl mt-1"
      >
        +
      </button>
    </div>
  );
};

export default Button;
